# -*- coding: utf-8 -*-
"""الصفحة الرئيسية لتطبيق EGX Analyzer."""

import streamlit as st
from api_client import inject_rtl_style, api_get

inject_rtl_style(st)

st.title("📈 EGX Analyzer")
st.markdown("### منصة شخصية لتحليل أسهم البورصة المصرية")

st.markdown("""
استخدم القائمة الجانبية للتنقل بين صفحات التطبيق:

- **📊 الأسهم** — أسعار الأسهم اللحظية وبياناتها
- **🚀 فرص صاعدة** — فحص تلقائي للأسهم اللي استوفت شروط الصعود الفنية
- **📰 الأخبار** — آخر أخبار البورصة المصرية
- **🎯 التوصيات** — تقرير تحليل شامل لأي سهم (فني + باك-تيستنج + بيانات أساسية)
""")

st.divider()

data, err = api_get("/")
if err:
    st.error(f"⚠️ الـ API مش شغال حاليًا: {err}")
    st.info("تأكد إنك شغّلت السيرفر في تيرمنال منفصل:\n\n`uvicorn app.api.main:app --reload --port 8000`")
else:
    st.success("✅ الاتصال بالـ API شغال تمام")

st.warning(
    "⚠️ هذا التطبيق أداة مساعدة للتحليل وليس نصيحة استثمارية. "
    "أي توصية هنا مبنية على مؤشرات فنية تاريخية ولا تضمن أي نسبة نجاح مستقبلية."
)
